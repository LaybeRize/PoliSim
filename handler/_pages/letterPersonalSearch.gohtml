{{define "pageScript"}}
    <script>
        window.onload = function () {
            document.body.addEventListener("htmx:afterRequest", function (evt) {
                if (evt.failed) {
                    alert({{/*letterPersonalSearch*/}}"Die Anfrage hat zu einem Fehler auf der Serverseite geführt")
                }
            })
        }
    </script>
{{end}}
{{define "page"}}
    {{- /*gotype: PoliSim/handler.SearchLetterPage*/ -}}
    <body id="page-body">
    {{template "header" .NavInfo}}
    {{$acc := .NavInfo.Account}}
    <form id="search-form" hx-put="/my/letter" hx-target="#page-body">
        <label for="account">{{/*letterPersonalSearch*/}}Account</label>
        <select id="account" name="account">
            <option value="">{{/*letterPersonalSearch*/}}-- Alle Accounts --</option>
            {{$account := .Account}}
            {{range .PossibleAccounts}}
                <option value="{{.}}" {{if eq . $account}}selected{{end}}>{{.}}</option>
            {{end}}
        </select>
        <label for="amount">{{/*letterPersonalSearch*/}}Anzahl der Ergebnisse</label>
        <input id="amount" name="amount" type="number" value="{{.Amount}}" min="10" max="50">
        <button>{{/*letterPersonalSearch*/}}Suchen</button>
    </form>
    <div class="wrapper note">
        {{if eq (len .Results) 0}}
            <p>{{/*letterPersonalSearch*/}}Es konnten keine Einträge gefunden werden, die den Suchkriterien gerecht werden</p>
        {{else}}
            {{range .Results}}
                {{- /*gotype: PoliSim/database.ReducedLetter*/ -}}
                <a href="/view/letter/{{.ID}}?viewer={{urlquery .Recipient}}" target="_blank"
                   {{if .Viewed}}class="read"{{end}}>
                    {{- if .Viewed -}}<i class="bi bi-letter-open"></i>
                    {{- else -}}<i class="bi bi-letter-closed"></i>
                    {{- end}} {{/*letterPersonalSearch*/}}<strong>{{.Title}}</strong> von {{.Author}}<br>
                    {{/*letterPersonalSearch*/}}Empfänger: {{.Recipient}}<br>
                    {{/*letterPersonalSearch*/}}<i>Versendet am: {{.GetTimeWritten $acc}}</i>
                </a>
            {{end}}
        {{end}}
        {{if or .HasNext .HasPrevious}}
            <hr style="width: var(--standard-width); margin: 0.3rem 0;">
            <div class="wrapper note pager">
                {{if .HasPrevious}}
                    <a hx-put="/my/letter" hx-include="#search-form"
                       hx-vals='{"page": {{.PreviousPage}}}' hx-target="#page-body">
                        {{/*letterPersonalSearch*/}}&laquo; Vorherige Seite
                    </a>
                {{end}}
                {{if .HasNext}}
                    <a hx-put="/my/letter" hx-include="#search-form"
                       hx-vals='{"page": {{.NextPage}}}' hx-target="#page-body">
                        {{/*letterPersonalSearch*/}}Nächste Seite &raquo;
                    </a>
                {{end}}
            </div>
        {{end}}
    </div>
    </body>
{{end}}